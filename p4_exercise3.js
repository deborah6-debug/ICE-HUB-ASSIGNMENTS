// 1. Create a Boolean value for an ID variable
let hasID = true; // change to false to test

// 2. Use ternary operator to check if ID is valid
let message = hasID 
  ? "✅ You may enter the venue." 
  : "⛔ Sorry, you cannot enter the venue.";

// 3. Output the response
console.log(message);
